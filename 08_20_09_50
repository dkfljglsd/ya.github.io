cd /d "배포폴더\app" && python -m venv .venv && .venv\Scripts\activate.bat && python -m pip install —no-index —find-links "..\wheels" fastapi uvicorn websockets numpy scipy pydantic pydantic-settings httpx faster-whisper

cd /d "배포폴더\app" && .venv\Scripts\activate.bat && python run.py
